fun main() {
    val x = readln().toBoolean() // read other values in the same way
    val y = readln().toBoolean() // read other values in the same way
    val z = readln().toBoolean() // read other values in the same way

    // write your code here
    print( !(x && y) || z)
}